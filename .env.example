STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# Optional: after `next build`, use first catalog artist on `/` when running `next start` locally
# (dev server already does this automatically)
# MUSICTIP_HOME_FIRST_ARTIST=true
